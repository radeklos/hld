spring:
  jpa:
    generate-ddl: true
    database: POSTGRESQL
    show-sql: false

  datasource:
    driverClassName: org.postgresql.Driver
    platform: postgres
    url: jdbc:postgresql://localhost:5432/holiday
    username: postgres
    password: postgres

  redis:
    host: localhost
